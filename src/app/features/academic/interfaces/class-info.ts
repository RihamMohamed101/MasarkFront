export interface ClassInfo { 
  classId: number; 
  gradeId: number; 
  gradeName: string; 
  name: string; 
  maxCapacity: number; 
  enrollmentCount: number; 
  academicYear: number;
  isActive: boolean;
}